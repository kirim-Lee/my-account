'use client';

import { useEffect } from 'react';

const Side = () => {
  useEffect(() => {
    // fetch('/api/account').then((res) => {
    //   // console.log(res);
    // });
  }, []);

  return <>hello</>;
};
export default Side;
